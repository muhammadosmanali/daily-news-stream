// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig :{
    apiKey: "AIzaSyCiPKJH21lGNFshi7OS1FWbWT14MjUxUmU",
    authDomain: "daily-news-stream.firebaseapp.com",
    databaseURL: "https://daily-news-stream.firebaseio.com",
    projectId: "daily-news-stream",
    storageBucket: "daily-news-stream.appspot.com",
    messagingSenderId: "360087849346",
    appId: "1:360087849346:web:b6f62d063abeefe415a79b",
    measurementId: "G-BBGVSKMRFL"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
